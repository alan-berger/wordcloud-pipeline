# wordcloud-pipeline
An automated pipeline that polls a remote URL for new incrementally-numbered text files, generates a wordcloud image from each one, and optionally patches metadata into an HTML/PHP page and sends a push notification via ntfy.
